spring-boot:build-image

docker build -t springexample .

docker ps
docker stop "id"

# Open shell in postgres docker instance
docker exec -it pg-docker /bin/bash

psql -U postgres -d local

http://localhost:8080/h2-console